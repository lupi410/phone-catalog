# phone-catalog

Phone catalog is a ReactJS app that consumes a NodeJS backend with in-memory data.

## RUN API

`cd api`
`npm install`
`npm run dev`

## Then, RUN APP

`cd ..`
`cd front`
`npm install`
`npm start`
